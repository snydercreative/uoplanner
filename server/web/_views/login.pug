doctype html

html(lang="en-US" charset="utf-8" ng-app="skillsApp")

	include includes/head-login.pug

	body.flex.flex-column(ng-cloak ng-controller="ShareCtrl as share")
			
		header
			h1 UOPlanner
			#skillcap For alt-o-holics, by alt-o-holics.
			include includes/header-menu.pug

		main.flex.flex-row.flex-justify-center
			.panel.login-panel
				#okta-login-container

		include includes/footer.pug

		script
			| var orgUrl = 'https://dev-724720.oktapreview.com';
			| var oktaSignIn = new OktaSignIn({baseUrl: orgUrl});
			| oktaSignIn.renderEl({ el: '#okta-login-container' }, function (res) {
			|	if (res.status === 'SUCCESS') { res.session.setCookieAndRedirect(orgUrl); }
			| });
